/** Props for the landing page footer CTA section. */
import type { ReactNode } from "react";

export interface CtaFooterSectionProps {
  /** Primary CTA (sign-in button or link to app). */
  primaryCta: ReactNode;
}
